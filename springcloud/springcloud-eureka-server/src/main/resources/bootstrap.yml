spring:
  application:
    name: eureka-server

server:
  port: 9010

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:9000/eureka/,http://localhost:9000/eureka/

logging:
  level:
    root: info

